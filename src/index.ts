//tipos primitivos: number, strings, boolean, object, undefined...

let nome: string;
nome = "Gabriel"

const frutas: string[] = [];

frutas.push("banana");
frutas.push("morango");

const pessoa: [string, number] = ["Gabriel", 34]; // dupla, conjunto de dados carregados juntos.

console.log(frutas);
console.log(pessoa);